# Check for Understanding: Operators

# Check for Understanding: Operators

## ![](https://moringa.instructure.com/courses/1391/files/620140/preview?) Check for Understanding: Operators

Icon Progress Bar (browser only)

Now is your opportunity to see how much you can recall from the topic! Take a moment to answer the questions below. These are not graded, but they will allow you to check how much you are putting together the content in this lesson and review before moving to a new topic. 

### Quick Check: Defining Control Flow Operator

What is the definition of a control flow operator like If-elif-else statements in Python? How are these operators commonly used for in programming?

Control flow operators are used to make decisions and execute different blocks of code based on certain conditions. Allowing programmers to create branching paths in programs where the execution flow can take different routes depending on the evaluation of one or more conditions.
:   **Correct:** Correct, an example of a control flow statement would be:

    ```
    number = 0

    if number > 0:
        print('Positive number')

    elif number <0:
        print('Negative number')

    else:
        print('Zero')

    print('This statement is always executed')
    ```

Control flow operators perform arithmetic calculations. Allowing developers to perform mathematical operations like addition, subtraction, multiplication, division, and more.
:   Incorrect, re-read the what are operators page.

Control flow operators are crucial for assigning values to variables. Allowing developers to store and manipulate data throughout the program.
:   Incorrect, re-read the what are operators page.

Check Answer

### Quick Check: Login System

Sarah is developing a login system for a website. She needs to verify that the password entered by a user matches the stored password for their account. To accomplish this, she will use a function that utilizes the comparison operator == to check if the passwords match.

Sarah has written the following function to compare the user-entered password with the stored password:

```
def equal_to(a, b):  
    return a == b
```

Sarah wants to test her function by comparing a user-entered password with the stored password. The stored password is "securePassword123", and the user has entered "securePassword123".

How should Sarah test her function?

equal\_to("securePassword123", "securePassword123")
:   **Correct:** The `equal_to` function uses the `==` comparison operator to check if the two input strings are exactly the same. In this case, both the stored password and the user-entered password are `"securePassword123"`, so the function will return `True`.

qual\_to("securePassword123", "SecurePassword123")
:   Incorrect, re-read the defining operators page.

equal\_to("securePassword", "securePassword123"
:   Incorrect, re-read the defining operators page.

Check Answer

### Quick Check: Manage Inventory

John is writing a Python program to manage inventory for a small bookstore. He needs to update the stock levels, calculate sales, and determine if the store needs to reorder books. The program should perform the following tasks:

1. Add new stock to the existing inventory.
2. Calculate the total sales value after selling a certain number of books.
3. Determine if the stock level for any book falls below the reorder threshold.

Which comparison operator should John use to determine if the stock level is equal to or falls below the reorder threshold after selling the books?

:   Incorrect, reread defining operators page.

==
:   Incorrect, reread defining operators page.

!=
:   Incorrect, reread defining operators page.

>=
:   **Correct:**The <= operator checks if the value on the left side is less than or equal to the value on the right side. In this scenario, John needs to determine if the stock level after selling the books is less than or equal to the reorder threshold to trigger a reorder.

Check Answer

---

# Source Information

Original URL: https://moringa.instructure.com/courses/1391/modules/items/239153

Scraped At: 2026-05-14T15:20:01.442060
